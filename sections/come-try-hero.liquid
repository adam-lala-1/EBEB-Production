<section class="eeb-section eeb-section--cream" style="padding: clamp(1.5rem, 3vw, 3rem) 0;">
  <div class="eeb-container" style="max-width: 1100px; text-align: center;">
    {% if section.settings.eyebrow != blank %}
      <span class="eeb-eyebrow">{{ section.settings.eyebrow }}</span>
    {% endif %}
    <h1>{{ section.settings.heading }}</h1>
    {% if section.settings.subheading != blank %}
      <p style="font-size: 1.1rem; color: var(--color-muted); max-width: 680px; margin: 1rem auto 2rem;">{{ section.settings.subheading }}</p>
    {% endif %}
    <div style="display:flex;gap:1rem;justify-content:center;flex-wrap:wrap;{% if section.settings.image != blank %}margin-bottom:2.5rem;{% endif %}">
      {% if section.settings.cta1_text != blank %}
        <a class="eeb-btn eeb-btn--primary" href="{{ section.settings.cta1_url | default: '#rsvp' }}">{{ section.settings.cta1_text }}</a>
      {% endif %}
      {% if section.settings.cta2_text != blank %}
        <a class="eeb-btn eeb-btn--dark" href="{{ section.settings.cta2_url | default: '#events' }}">{{ section.settings.cta2_text }}</a>
      {% endif %}
    </div>
    {% if section.settings.image != blank %}
      <div style="border-radius:14px;overflow:hidden;box-shadow:0 20px 50px -20px rgba(27,42,74,.35);">
        <img src="{{ section.settings.image | image_url: width: 1600 }}" alt="{{ section.settings.image.alt | default: section.settings.heading | escape }}" loading="eager" style="display:block;width:100%;height:auto;">
      </div>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Come & Try hero",
  "settings": [
    { "type": "image_picker", "id": "image", "label": "Hero image" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Enquiries & Test Rides" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Come and Try — or just ask" },
    { "type": "textarea", "id": "subheading", "label": "Subheading", "default": "The best way to find the right ebike or trike is to ride one. Book a free test ride at our Gaythorne workshop any weekday, join us at an upcoming event, or send us a question — whatever suits you best." },
    { "type": "text", "id": "cta1_text", "label": "Primary CTA text", "default": "Book a test ride" },
    { "type": "url", "id": "cta1_url", "label": "Primary CTA URL" },
    { "type": "text", "id": "cta2_text", "label": "Secondary CTA text", "default": "See upcoming events" },
    { "type": "url", "id": "cta2_url", "label": "Secondary CTA URL" }
  ],
  "presets": [{ "name": "Come & Try hero" }]
}
{% endschema %}
